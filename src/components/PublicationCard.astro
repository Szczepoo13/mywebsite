---
interface Props {
	title: string;
	authors: string;
	venue: string;
	date: Date;
	url?: string;
}

const { title, authors, venue, date, url } = Astro.props;
---

<div class="card px-5 py-4">
	<h2 class="text-base font-bold tracking-tight">
		{
			url ? (
				<a href={url} class="text-accent-blue hover:underline" target="_blank" rel="noreferrer">
					{title}
				</a>
			) : (
				title
			)
		}
	</h2>
	<p class="mt-1 text-sm text-muted">{authors}</p>
	<p class="mt-1 text-xs text-muted">{venue} · {date.getFullYear()}</p>
</div>
